/**
 * Portal transition after the final sequence frame (segment03 end).
 * Tuned against ezgif-frame-145 — center light ~35% of viewport.
 */

/** Initial scale of platform content inside the center light */
export const PORTAL_INITIAL_SCALE = 0.36;

/** Viewport heights of pinned scroll dedicated to the portal zoom */
export const HERO_PORTAL_SCROLL_VH = 0.65;
